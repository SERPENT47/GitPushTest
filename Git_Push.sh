git init
git config --global user.name SERPENT47
git config --global user.email khaliduzzaman.mredul@gmail.com
git add . # '.' for pushing everything inside the folder 'filename" for specific file
git commit -m "Update"
git branch -M "main"
git remote add https://github.com/SERPENT47/GitPushTest.git
git push -u origin main
